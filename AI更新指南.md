# 日程 · AI 更新指南

这份指南供 Codex 或其他 AI 读取。目标是把用户的一段自然语言记录转换为应用可预览的更新，不直接修改应用数据库。

## 工作方式

1. 读取用户在应用「AI 更新」中复制的最新上下文，或用户指定的本地数据文件。
2. 根据用户原话生成一份 UTF-8 编码的 JSON 更新文件。
3. 让用户把 JSON 粘贴到应用；若可以访问应用目录，也可以把文件写入 `data/inbox`，例如 `2026-09-22-evening.json`。
4. 用户在应用内检查变更预览，再点击应用。

只创建更新文件。不要直接覆盖 `data` 中的主数据、备份或已经处理的文件。每份独立更新使用新的文件名，避免旧内容与新内容混淆。

## 理解用户原话

- **完成具体任务**：只更新该任务；不要推断整个节点已完成。
- **结束当前节点并开始下一节点**：只有用户明确说明时，才推进主线当前位置。
- **计划做某事**：添加待办，保持未完成。
- **想法或模糊的活动记录**：写成便利贴，不猜测完成状态或次数。
- **关键日程与准备事项**：支持多个单次、每周、每月安排。保留用户给出的名称、重复规则、当地时间与事项截止日期，不自行推算。

“今天做了贪心”不等于“贪心节点已完成”。“研究了登录流程”不等于“登录模块已实现”。“投了三家公司”可以记录用户明确给出的活动，但不要编造公司名、结果或面试状态。

将“今天”“明天”转换为用户本地日期，以最新上下文给出的日期为准。已有记录优先复用，不重复创建同名任务；同名可能指向多个节点时，使用上下文确定对象，无法确定就请求用户补充或保留原文为笔记。

## 保持更新可核对

- 使用上下文中的真实 ID 标识已有对象，不凭空生成已有对象的 ID。
- 每个操作只表达一个变化；在交付说明中用中文说明这次更新，不要向 JSON 添加额外字段。
- 未得到用户明确表达时，不删除记录、不完成整个主线、不更改关键日期的名称或日期。
- 主线名称、数量和路线由用户自定义。首次使用的主线列表为空；升级后以现有数据为准，不假定一定存在求职、GitHub、LeetCode 或八股主线。
- 新建路线不代表用户完成过任何节点。不要把路线规划当成历史成就。
- 输出供应用读取的文件时只写 JSON，不加 Markdown 代码围栏或额外解释。

## JSON 格式

更新文件与上下文是两种不同格式。上下文中包含 `schemaVersion`、`today`、`operationSchema` 与 `state`；这些字段不要整体复制进更新文件。`state` 内使用 `Id`、`Title`、`Tracks`、`Nodes` 等大写开头的字段。更新文件使用下面的小写字段，名称区分大小写。

```json
{
  "id": "2026-09-22-evening-8b02f4",
  "source": "Codex · 晚间记录",
  "operations": [
    {
      "type": "add_note",
      "date": "2026-09-22",
      "text": "项目介绍还需要更清晰地说明解决了什么问题。"
    }
  ]
}
```

- `id`：必填，1–160 个字符。每份新更新使用新的唯一字符串或 UUID；同一份更新重试时保留原 ID，应用会拒绝重复导入。
- `source`：可选，最多 160 个字符。表示来源，默认 `AI 更新`。
- `operations`：必填，包含 1–200 个操作，按数组顺序执行。JSON 文本最多 2,097,152 个字符。
- 日期使用 `yyyy-MM-dd`。布尔值必须是 JSON 的 `true` 或 `false`，不能写成字符串。可选字段不使用时直接省略，不传 `null`。
- 下表之外的操作和字段会被拒绝。任意操作无效时，整份更新都不会应用。

### 支持的操作

每个操作都必须有 `type` 字段。以下“必填”和“可选”不重复列出 `type`。

| `type` | 必填字段 | 可选字段与含义 |
|---|---|---|
| `add_track` | `title` | `nodes` 为初始节点名称的字符串数组，可省略或为空数组 |
| `rename_track` | `trackId`、`title` | 修改主线名称 |
| `upsert_application` | `trackId`、`company`、`status` | 状态取 `planned`（待投）、`applied`（已投）、`assessment`（笔试）、`interview`（面试）、`offer`、`rejected`（未通过）、`watch`（观望）、`no_response`（暂无回复）。可选 `applicationId`、`role`、`appliedDate`（yyyy-MM-dd 或空）、`notes`。不提供 ID 时按同一主线的公司与岗位匹配并更新，找不到才新增。缺少的日期不得猜测。 |
| `set_applications_source` | `trackId`、`url` | 保存原始投递清单的 http/https 链接；空字符串清除。仅用于打开原表，不自动同步。 |
| `add_node` | `trackId`、`title` | 在已有主线末尾添加节点 |
| `rename_node` | `trackId`、`nodeId`、`title` | 修改节点名称 |
| `add_todo` | `title` | `date` 默认今天；`done` 表示完成状态；关联已有主线时同时提供 `trackId`、`nodeId`，并可提供 `nodeTaskId` |
| `complete_todo` | `todoId` | `done` 默认 `true`；`false` 表示重新打开 |
| `add_task` | `trackId`、`nodeId`、`title` | `done` 默认 `false`；用户明确做完时可设为 `true` |
| `rename_task` | `trackId`、`nodeId`、`taskId`、`title` | 修改具体任务名称；保留完成状态 |
| `complete_task` | `trackId`、`nodeId`、`taskId` | `done` 默认 `true`；`false` 表示重新打开 |
| `set_current_node` | `trackId`、`nodeId` | 把此节点设为主线当前位置 |
| `complete_node` | `trackId`、`nodeId` | 完成整个节点及其所有子任务和关联待办 |
| `add_note` | `text` | `date` 默认今天；`color` 为 `#RRGGBB` 颜色 |
| `set_milestone` | `title` | `date` 省略时保留原日期，空字符串清除日期；名称最多 100 个字符 |
| `add_departure` | `title` | 向准备清单添加事项；`dueDate` 为截止日期，省略表示无截止日期 |
| `complete_departure` | `departureId` | 更新准备事项；`done` 默认 `true`，`false` 表示重新打开 |
| `set_departure_date` | `date` | 修改当前关键日期；`date` 设为空字符串可清除日期 |
| `add_event` | `title` | `repeat` 为 `none`（默认）、`weekly` 或 `monthly`；`time` 为 `HH:mm`（默认 `09:00`）；单次必填 `date`；每周必填 `weekDay`（0 周日至 6 周六）；每月必填 `monthDay`（1–31） |
| `update_event` | `eventId` | 可提供 `title`、`date`、`time`、`repeat`、`weekDay`、`monthDay`；省略的字段保留原值 |

主线、节点、任务和清单事项的标题最多 500 个字符，事件名称最多 100 个字符，便利贴正文最多 20,000 个字符。新增对象的 ID 由应用生成，不能在新增操作里自行指定。

### 关键日期与准备清单

应用支持多个关键日程。新建用 `add_event`，修改已有日程用 `update_event` 与上下文中的真实 `eventId`。每周和每月会自动计算下一次发生时间；每月 29–31 日在短月取月末，之后的月份继续使用原定日号。日程使用电脑的本地日期和时间。名称为 1–100 字，时间为 24 小时制。不要自动创建用户未提到的准备事项。

例如用户说“每周三下午三点组会”，可生成：

```json
{"type":"add_event","title":"组会","repeat":"weekly","weekDay":3,"time":"15:00"}
```

`set_milestone` 与 `set_departure_date` 保留兼容旧版单个倒计时，作用于专门的 `legacy-milestone` 项，不会覆盖其他新建日程。优先使用新的事件操作。

为兼容旧数据，状态中的事件日期仍叫 `DepartureDate`，准备事项仍在 `DepartureItems`，事件名称存放在 `MilestoneTitle`。旧的 `add_departure`、`complete_departure`、`set_departure_date` 操作继续有效，作用于当前关键事件及其通用准备清单，不局限于离境。

首次使用时，关键日程与准备清单为空。升级时，旧版已设置的离境日期会转成单次日程，原检查清单保留；不能据此判断用户想改名或清空安排。

### 创建与整理主线

用户可以创建任意主题的主线，例如学习、求职或个人项目。`add_track` 的 `nodes` 最多包含 200 个节点名称，每个名称为 1–500 个字符的非空字符串。主线名称在全部主线中唯一，节点名称在所属主线中唯一；名称比较不区分英文大小写，首尾空格会被去除。

例如，用户明确要求创建一条阅读计划时：

```json
{
  "id": "2026-09-23-reading-43e1c9",
  "source": "Codex · 新建阅读计划",
  "operations": [
    {
      "type": "add_track",
      "title": "阅读计划",
      "nodes": ["选择书目", "阅读与笔记", "整理总结"]
    }
  ]
}
```

新建主线有节点时，第一个节点设为 `current`，其余设为 `pending`；节点都没有已完成记录。不提供节点时，主线暂时为空。`add_node` 在主线没有任何节点时，将新节点设为当前；其他情况下，新节点为 `pending`，即使原主线当前没有活动节点，也不会自动推进。

新增主线和节点的 ID 需在应用更新后重新读取上下文。不要在同一份更新的后续操作中猜测它们的 ID。需要一次创建整条路线时，使用 `add_track` 的 `nodes`。

改名使用对应的 `rename_*` 操作，不通过删除再创建来替代，以保留记录与关联。`rename_task` 会同步改名通过准确 `NodeTaskId` 关联、且仍使用旧任务名的每日待办；用户另外取名的每日待办保持原名。改名不能与同范围内已有名称重复。

### 关联任务与当前位置

已有节点任务完成时，优先使用 `complete_task`；应用会同步完成关联的每日待办。完成关联待办也会同步更新节点任务。同名任务有歧义时，必须使用上下文里的准确 ID。

用户说“今天做完跳跃游戏”，但该节点尚未记录这道题时，使用 `add_task` 并设置 `done: true` 即可一次记入。不要先创建未完成任务，再猜测新生成的 ID。

`add_todo` 提供 `trackId`、`nodeId` 后，如果节点里有唯一的同名任务，会自动关联该任务；也可以明确提供 `nodeTaskId`。若任务还不存在，先用 `add_task` 创建，再用相同标题的 `add_todo` 关联，操作按这个顺序放进同一份更新。

新待办没有指定 `done` 时，关联了节点任务就沿用该任务的完成状态，否则为未完成。明确提供 `done` 时，也会同步到关联的节点任务。

完成子任务不会自动完成父节点。`complete_node` 会把该节点所有子任务与关联待办都标记完成，但不会自动开始下一个节点。

把已完成节点中的某个任务重新打开后，节点会回到 `pending`；应用不会因此自动改变主线的当前节点。

用户明确表示“贪心结束，开始动态规划”时，依次提交 `complete_node` 和 `set_current_node`。如果只提交 `set_current_node`，原来的当前节点会回到 `pending`，不会被视为完成。不要因为切换学习主题就替用户记录完成整章。

导入不支持删除记录或调整排列顺序，也不支持改写已有便利贴、每日待办或准备事项的文本。不要创造 `delete_*`、`edit_*`、`reorder_*` 等操作。

笔记本由用户在应用内通过「收工存档」收录便利贴。AI 仍使用 `add_note` 记录想法，不生成未支持的归档、编辑笔记本或删除笔记本操作。已归档内容是独立记录；相同来源便利贴的相同内容不重复添加，来源文字改变后会新增记录并保留旧版本。笔记本中的独立编辑不会被再次归档覆盖。

主线、节点与具体任务的删除在应用里操作。删除主线或节点后，关联的每日待办保留并解除关联；删除具体任务只清除 `NodeTaskId`，保留待办的主线与节点信息。删除当前节点不会自动开始下一节点。

## 本地数据与处理结果

- `data/state.json`：当前数据，仅作为上下文读取；不要直接覆盖。
- `data/state.backup.json`：上一次保存前的备份。
- `data/inbox/*.json`：等待在应用里预览的更新文件。
- `data/processed/`：已处理的更新文件归档。

应用预览后若原数据发生变化，需要重新预览再应用。撤销仅针对本次应用会话中的最近一次 AI 更新，而且期间不能有后续数据修改；不要把撤销视为完整版本历史。

附带的 `sample-update.json` 不依赖任何主线或任务 ID，仅添加一张示例便利贴，适合检查导入流程。
